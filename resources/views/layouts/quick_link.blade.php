<div class="sidebar sidebar-left mt-sm-30">
    <div class="widget" style="background-color: #ab7046; padding: 20px;">
        <h5 class="widget-title">
            @if (session('key') == 'jp')
                クイックリンク
            @else
                Quick Links
            @endif
        </h5>
        <ul class="list list-divider list-border">
            <li>
                <a href="#" class="text-white">
                    <i class="fa fa-check-square-o mr-10 text-white"></i>
                    @if (session('key') == 'jp')
                        ミャンマ—
                    @else
                        Myanmar
                    @endif
                </a>
            </li>

            <li>
                <a href="#" class="text-white">
                    <i class="fa fa-check-square-o mr-10 text-white"></i>
                    @if (session('key') == 'jp')
                        ミャンマ—
                    @else
                        Myanmar
                    @endif
                </a>
            </li>

            <li>
                <a href="#" class="text-white">
                    <i class="fa fa-check-square-o mr-10 text-white"></i>
                    @if (session('key') == 'jp')
                        ミャンマ—
                    @else
                        Myanmar
                    @endif
                </a>
            </li>

            <li>
                <a href="#" class="text-white">
                    <i class="fa fa-check-square-o mr-10 text-white"></i>
                    @if (session('key') == 'jp')
                        ミャンマ—
                    @else
                        Myanmar
                    @endif
                </a>
            </li>

            <li>
                <a href="#" class="text-white">
                    <i class="fa fa-check-square-o mr-10 text-white"></i>
                    @if (session('key') == 'jp')
                        ミャンマ—
                    @else
                        Myanmar
                    @endif
                </a>
            </li>
            <li>
                <a href="#" class="text-white">
                    <i class="fa fa-check-square-o mr-10 text-white"></i>
                    @if (session('key') == 'jp')
                        ミャンマ—
                    @else
                        Myanmar
                    @endif
                </a>
            </li>

            <li>
                <a href="#" class="text-white">
                    <i class="fa fa-check-square-o mr-10 text-white"></i>
                    @if (session('key') == 'jp')
                        ミャンマ—
                    @else
                        Myanmar
                    @endif
                </a>
            </li>

            <li>
                <a href="#" class="text-white">
                    <i class="fa fa-check-square-o mr-10 text-white"></i>
                    @if (session('key') == 'jp')
                        ミャンマ—
                    @else
                        Myanmar
                    @endif
                </a>
            </li>

            <li>
                <a href="#" class="text-white">
                    <i class="fa fa-check-square-o mr-10 text-white"></i>
                    @if (session('key') == 'jp')
                        ミャンマ—
                    @else
                        Myanmar
                    @endif
                </a>
            </li>
        </ul>
    </div>
</div>
